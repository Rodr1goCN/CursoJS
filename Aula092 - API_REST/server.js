import app from './app';

const port = 3001;
app.listen(port, () => {
  console.log();
  console.log(`Executando na porta ${port}, mano?`);
  console.log(`CTRL + click em http://localhost:${port}, EITAAAA?`);
});
