import _ from './módulo';

const array = [100, 200, 300, 400];

console.log(_.sum(array)); // Soma
console.log(_.min(array)); // Mínimo
console.log(_.max(array)); // Máximo
console.log(_.mean(array)); // Média
console.log(_.mul(array)); // Multiplicação
console.log(global.MINHAGLOBAL);
