//tipos de dados primitivos: String, Number, Null, Undefined, Boolean, Symbol (vamos ver mais para frente)

const nome = 'rodrigo'; //string
const nome1 = "rodrigo"; //string
const nome2 = `rodrigo`; //string
const num1 = 10; //Number
const num2 = 10.50; //Number
let nomeAluno; // undefined = não aponta para local nenhum na memória
let sobrenomeAluno = null; //nulo = não aponta para local nenhum na memória
const aprovado = false; //Boolean = true ou false (lógico)